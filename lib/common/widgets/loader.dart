import 'package:flutter/material.dart';

class Lodaer extends StatelessWidget {
  const Lodaer({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: CircularProgressIndicator());
  }
}
